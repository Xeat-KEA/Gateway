spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://USERSERVICE-LOCAL
          predicates:
            - Path=/auth/**, /health, /oauth2/**, /login/**, / #인증 제외 목록
        - id: user-service
          uri: lb://USERSERVICE-LOCAL
          predicates:
            - Path=/users/**
          filters:
            - AuthFilter
        - id: blog-service
          uri: lb://BLOGSERVICE-LOCAL
          predicates:
            - Path=/blogs/**
          filters:
            - AuthFilter

  jwt:
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb

server:
  port: 19091

# Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:19090/eureka

logging:
  level:
    org.springframework.security: TRACE


